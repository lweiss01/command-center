# S06: Import age reminders and portfolio card polish

**Goal:** In computeNextAction, add a check: if imports are stale (>14 days), readiness is ready, and no continuity/readiness blocker exists, add an import age suggestion to the action. Also surface import age on portfolio cards (the data is already there from M005).
**Demo:** After this: After this: a repo where imports are >14 days old and there is no harder blocker gets a clear Next Action suggestion to re-import, not just a buried health signal.

## Tasks
