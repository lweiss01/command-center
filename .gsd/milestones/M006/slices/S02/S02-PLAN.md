# S02: Add project by path

**Goal:** Enable the 'New' button in the UI. Accept a directory path, validate it exists and is a git repo or has planning artifacts, upsert it as a project, and trigger artifact detection for that path.
**Demo:** After this: After this: user can type a directory path into the 'New' input and add a project without running a full workspace scan.

## Tasks
