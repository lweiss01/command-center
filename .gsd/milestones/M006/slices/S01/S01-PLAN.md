# S01: Auto-import on scan

**Goal:** After a successful scan, for each discovered project that has gsd_project/gsd_requirements/gsd_decisions artifacts detected, automatically run the corresponding imports. Skip if already recently imported (within 24h).
**Demo:** After this: After this: running a scan automatically imports planning data for any discovered project that has GSD docs — no manual import buttons required for fresh projects.

## Tasks
