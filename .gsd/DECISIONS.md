# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? | Made By |
|---|------|-------|----------|--------|-----------|------------|---------|
| D001 | M002 bootstrap discussion | integration | Standard workflow stack to audit for readiness | Treat GSD, GSD2, Beads, Holistic, required repo docs, and callable local tools as the standard workflow stack for readiness checks. | Workflow readiness is only useful if it reflects the actual substrate the user relies on across repos. | Yes — if the user changes the standard workflow stack. | human |
| D002 | M002 bootstrap discussion | architecture | Primary durable source of truth | Keep repo-local planning and continuity artifacts as the durable source of truth, with Command Center maintaining an imported/interpreted model that remains visibly secondary. | The product exists to preserve inspectable repo memory and avoid hidden-state or Mission Control-style complexity. | Yes — if a future capability cannot be represented durably in repo-local artifacts without harming usability. | collaborative |
| D003 | M002 bootstrap discussion | product | Post-M002 milestone sequencing | Sequence the roadmap as resume-first cockpit (M002), then workflow bootstrap/authoring (M003), then validation/proof (M004), then drift repair/portfolio prioritization (M005+). | Trustworthy understanding and resume guidance must exist before the product starts writing workflow state or judging proof. | Yes — if later execution shows the sequence creates avoidable friction. | collaborative |
| D004 | M002 bootstrap discussion | integration | Standard workflow stack to audit for readiness | Treat GSD, GSD2, Beads, Holistic, required repo docs, and callable local tools as the standard workflow stack for readiness checks. | Workflow readiness is only useful if it reflects the actual substrate the user relies on across repos. | Yes — if the user changes the standard workflow stack. | human |
| D005 | M001 planning backfill | architecture | M001 planning scope and contract | Treat M001 as the import-first foundation milestone centered on the single backend project snapshot contract (`/api/projects/:id/plan`), with deeper workflow-readiness and resume semantics deferred to M002. | The implemented M001 work establishes discovery, artifact detection, canonical import/provenance, and cockpit rendering through one repo-backed import path. Preserving that boundary avoids retroactively inflating M001 scope and keeps later workflow interpretation subordinate to imported repo truth. | Yes | agent |
| D006 | M002/S01 execution | interpretation | computeWorkflowState confidence model | Use additive fixed-increment confidence (milestone +0.15, requirements +0.20, decisions +0.10, import recency up to +0.25, continuity freshness up to +0.30). | Explainable and conservative by design. Weighted-average or ML-style scoring would undermine the "no magic" philosophy of the interpretation contract. | Yes — if the signal set or philosophy changes. | agent |
| D007 | M002/S01 execution | interpretation | nextAction priority field dropped in favor of blockers[] | Replaced the priority string field on nextAction with blockers[]. Non-empty blockers is a clearer binary than high/medium/low. | priority is a label; blockers[] is an explicit, testable list of things preventing normal work. Dropped fields should not be re-added without reviewing the broader contract. | Yes — if a multi-level urgency signal becomes genuinely needed. | agent |
| D008 | M002/S01 execution | interpretation | Continuity status uses three values: 'fresh', 'stale', 'missing' | No fourth status ('aging', 'ok', etc.). | Three values is unambiguous and forces display decisions into the UI layer where they belong. | Yes — only if a genuinely distinct fourth state is proven necessary. | agent |
| D009 | M002/S02 execution | integration | Holistic checkpoint timestamp key-path | Read `holisticState.passiveCapture?.lastCheckpointAt` as primary, `holisticState.lastAutoCheckpoint` as fallback. Top-level `lastCheckpointAt` and `lastHandoffAt` do not exist in state.json. | Incorrect key-path caused every repo with Holistic state to show checkpointHygiene:'stale' with 'No explicit checkpoint timestamp found' even when a recent auto-checkpoint existed. | Yes — if Holistic state schema changes. | agent |
| D010 | M002/S02 execution | interpretation | handoffCommand is injected server-side, not derived in the UI | The server computes the platform-correct handoff command string (`process.platform==='win32'` check) and injects it into the continuity struct. | Server-side injection makes the platform check authoritative and keeps the UI stateless. All three computeContinuity return paths include handoffCommand for consistent API shape. | Yes | agent |
| D011 | M002/S02 execution | interpretation | stale continuity + ok/stale hygiene is a soft reminder, not a hard blocker | Split the stale-continuity branch in computeNextAction: stale+missing hygiene pushes a hard blocker; stale+ok or stale+stale hygiene only surfaces a hygieneNote (blockers[] empty). | Treating any stale continuity as a hard blocker was too aggressive — active repos with recent passive captures should not be blocked from normal next-step guidance. | Yes — if product philosophy changes around blocking. | agent |
| D012 | M002/S05 execution | architecture | toolOverrides pattern on computeReadiness for portfolio route | Added optional `toolOverrides?: { holisticStatus; gsdStatus }` to computeReadiness. When present, those values are used instead of calling execFileSync. | The portfolio route iterates N projects — calling execFileSync per-project would fire O(N) slow subprocess probes. The override allows one probe at route entry and reuse across all projects. | Yes — if computeReadiness call sites change significantly. | agent |
| D013 | M002/S05 execution | interpretation | computeUrgencyScore uses additive fixed increments, capped at 1.0 | +0.40 fresh continuity, +0.25 unresolved requirements, +0.20 stalled/no-data (non-missing continuity), +0.15 readiness gaps. | Consistent with the explainable scoring philosophy established in D006. Avoids normalization or weighted math that would make the score opaque. | Yes — if urgency signal set or philosophy changes. | agent |
